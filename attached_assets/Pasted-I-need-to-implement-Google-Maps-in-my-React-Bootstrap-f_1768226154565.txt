I need to implement Google Maps in my React + Bootstrap food donation project. 

**Project:** ReNourish - Food redistribution system
**Tech:** React, Bootstrap 5, Spring Boot backend
**Need:** Map for donors to select locations, NGOs to view donations on map, and volunteers to see routes.

**Requirements:**
1. Location picker with autocomplete in donation form
2. Map showing multiple donation markers
3. Radius-based filtering
4. Route visualization for deliveries
5. Bootstrap 5 responsive design

**Current structure:**
- Already have latitude/longitude in database
- Backend API returns coordinates
- Need frontend map components

Please provide:
1. Best React Google Maps library for Bootstrap
2. Setup instructions
3. Complete Map component code with Bootstrap styling
4. Integration with forms
5. Responsive implementation
For Specific Component Help:
For Location Picker Component:
text
I need a Google Maps location picker component for my React Bootstrap project. The component should:

1. Show an interactive map
2. Have an address search bar with autocomplete
3. Allow clicking/dragging to select location
4. Display selected coordinates
5. Integrate with React Hook Form
6. Use Bootstrap 5 styling classes

Current props needed:
- `onLocationSelect: (lat: number, lng: number, address: string) => void`
- `initialLocation?: { lat: number, lng: number }`
- `label?: string`

Please provide complete component code.
For Donations Map Component:
text
I need a map component to display multiple donation locations in my food donation app.

Requirements:
- Display multiple markers (donation locations)
- Cluster markers when zoomed out
- Show info window on marker click with donation details
- Filter markers by radius
- Search locations
- Bootstrap 5 responsive design

Data structure:
```typescript
interface Donation {
  id: number;
  latitude: number;
  longitude: number;
  foodType: string;
  quantity: number;
  city: string;
  status: string;
}